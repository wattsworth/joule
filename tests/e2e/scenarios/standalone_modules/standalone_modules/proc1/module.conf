[Main]
name = plus2
description = adds 3 to input
exec_cmd = /module_scripts/adder.py 3

[Inputs]
# this already exists
input = /counting/base

[Outputs]
# this is configured in the streams_config folder
output = /counting/plus3

