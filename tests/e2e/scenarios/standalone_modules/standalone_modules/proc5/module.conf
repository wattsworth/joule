[Main]
# exec_cmd not used because the filter is run as a standalone process
exec_cmd = joule-visualizer-filter 
name = Data App
is_app = yes

[Arguments]
title = Quick Start Data Pipeline

[Inputs]
base = /counting/base
