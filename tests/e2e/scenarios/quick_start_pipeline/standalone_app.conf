[Main]
# exec_cmd is not used since this is a standalone app
exec_cmd = joule-visualizer-filter
name = Standalone App
is_app = yes

[Arguments]
title = Standalone App

# no inputs, will create mock data